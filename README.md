# WinForms-DualViewRoIComparator
Windows Forms tool that visualizes a single video in a synchronized 3-split view (original / ROI overlay / spatial-density heatmap) with a real-time ROI-vs-background estimated-bits graph. Supports headerless raw .yuv (I420) input and JSON/PNG session CRUD.
